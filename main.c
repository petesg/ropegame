#include <stdio.h>
#include <stdbool.h>

#include "actors.h"

int main(int argc, char *argv[]) {

    // initialization
    initActors();

    printf("Hello World!");

    // game loop
    bool exit = true;
    while (!exit) {

    }

    // cleanup

    return 0;
}
